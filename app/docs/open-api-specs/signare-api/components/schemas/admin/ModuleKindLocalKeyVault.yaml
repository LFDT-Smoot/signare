type: object
additionalProperties: false
properties:
  hsmKind:
    type: string
    enum:
      - LocalKeyVault
    x-required: mandatory
    nullable: false
    description:
      The kind of HSM
required:
  - hsmKind

